이터러블은 메서드 Symbol.iterator가 구현된 객체이다.
배열, 문자열은 대표적인 내장 이터러블이다.
여기서 배열이 내장 이터러블이라해서 이터러블 객체가 배열인 것은 아니다.
이터러블 객체는 배열을 일반화한 객체이다.

일반적인 객체는 이터러블이 아니며 일반객체를 이터러블 객체로 만들기 위해선 Symbol.iterator가 구현되어야 한다.

유사 배열(array-like) 은 인덱스와 length 프로퍼티가 있어서 배열처럼 보이는 객체를 의미한다.

이터러블, 유사 배열 둘 다인 객체가 존재하나 이터러블이라 해서 유사배열이 아니며 그 반대 또한 마찬가지이다.
이터러블의 경우 인덱스와 length가 없을 경우 유사배열이 될 수 없으며, 유사배열은 인덱스와 length가 있지만 Symbol.iterator가 없을 경우 이터러블이 될 수 없다.
또한 유사배열은 배열과 유사한 특징을 가지고 있을 뿐이지 배열이 아니다.

for문으론
이터러블 객체의 경우 for of 사용이 적절하다.

for문으론 foreach,for of, for in 있는데

for of의 경우 value 값을 받아 온다는 특징이 있고
for in의 경우 key 값을 받아 온다는 특징이 있다.

일반적으로 봤을 때 for of의 경우엔 이터러블 객체, for in의 경우 일반 객체에 사용이 적합해 보인다.
